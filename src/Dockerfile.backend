# Dockerfile.backend
FROM node:18-alpine AS build
WORKDIR /app
COPY backend/package*.json ./
RUN npm ci --production
COPY backend ./
# If you need a build step, run it here (e.g. transpile)
# RUN npm run build

EXPOSE 4000
CMD ["node", "server.js"]   # adjust to your server entry point
